---
# Example landing page entry — seed file (task #207, 2026-05-16).
#
# Kept as draft: true so it never emits a route, but its presence quiets
# the "glob-loader: No files found" warning and the "collection is empty"
# message during build. Delete this file once a real landing page exists.
#
# Schema reference: astro/src/content.config.ts (`landing-pages`).
# Sveltia editor: astro/public/admin/config.yml (`landing-pages`).
title: Example landing page (do not delete until first real LP exists)
slug: example-do-not-publish
draft: true
---

This is a seed entry. It will not appear on the live site because `draft: true`.
Once Mark creates a real landing page through Sveltia, this file can be removed.
