# Build output — regenerated by `bundle exec jekyll build`.
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby toolchain
.bundle/
vendor/

# Node (if a build pipeline is added later)
node_modules/

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Large binary asset directory — currently kept in Dropbox (already version-controlled via Dropbox history).
# Git-LFS migration is a website-redev project task; until then, do not `git add assets/`.
assets/
